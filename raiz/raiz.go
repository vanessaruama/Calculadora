//Package raiz faz a raiz quadrada de um número inteiro
package raiz

import (
	"math"
)

//Sqrt retorna a raiz quadrada de um número inteiro
func Sqrt(x float64) float64 {
	result := math.Sqrt(x)
	return result
}
